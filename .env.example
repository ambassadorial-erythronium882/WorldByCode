# Recommended for self-hosting. The Settings panel can also use a temporary
# session key when this server-side value is empty.
OPENAI_API_KEY=

# Optional. Defaults to the quality-first GPT-5.6 alias.
OPENAI_WORLD_MODEL=gpt-5.6
